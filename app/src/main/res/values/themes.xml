<resources>
    <!-- Compose draws everything; the platform theme only covers the window before it does. -->
    <style name="Theme.WebLauncher" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
